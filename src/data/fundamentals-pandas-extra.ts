import type { FundTopic } from "./fundamentals";

export const pandasExtraTopics: FundTopic[] = [
  {
    slug: "indexing-selection",
    subject: "pandas",
    title: "Indexing & Selection",
    year: 2025,
    authors: ["PaperCode Team"],
    category: "PANDAS",
    description: "Master label-based (loc) and position-based (iloc) indexing, MultiIndex, and advanced selection patterns.",
    tasks: [
      {
        slug: "loc-iloc",
        title: "loc vs iloc",
        shortDescription: "Select rows and columns using label-based and position-based indexers.",
        difficulty: "easy",
        category: "Core",
        description: `# loc vs iloc\n\n## Task\nImplement \`loc_iloc_ops(df)\` returning:\n1. Rows with index label >= 2 using \`loc\`\n2. First 2 rows using \`iloc\`\n3. Value at row 0, column 'score' using \`iloc\``,
        skeleton: `import pandas as pd\n\ndef loc_iloc_ops(df: pd.DataFrame):\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import pandas as pd\ndef test_loc():\n    df=pd.DataFrame({'score':[10,20,30,40]},index=[0,1,2,3])\n    a,b,c=loc_iloc_ops(df)\n    assert len(a)==2 and len(b)==2 and c==10\n    print("All tests passed!")`,
      },
      {
        slug: "multiindex",
        title: "MultiIndex Basics",
        shortDescription: "Create and slice a MultiIndex DataFrame.",
        difficulty: "medium",
        category: "Core",
        description: `# MultiIndex\n\n## Task\nImplement \`multi_idx(df)\` that:\n1. Sets ['region','category'] as a MultiIndex\n2. Returns all rows for region 'North' using \`xs\``,
        skeleton: `import pandas as pd\n\ndef multi_idx(df: pd.DataFrame):\n    # return (mi_df, north_rows)\n    raise NotImplementedError`,
        tests: `import pandas as pd\ndef test_mi():\n    df=pd.DataFrame({'region':['North','North','South'],'category':['A','B','A'],'val':[1,2,3]})\n    mi,north=multi_idx(df)\n    assert isinstance(mi.index,pd.MultiIndex)\n    assert len(north)==2\n    print("All tests passed!")`,
      },
    ],
  },
  {
    slug: "merging-joining",
    subject: "pandas",
    title: "Merging & Joining",
    year: 2025,
    authors: ["PaperCode Team"],
    category: "PANDAS",
    description: "Combine DataFrames with merge, join, and concat — inner, outer, left, and right joins.",
    tasks: [
      {
        slug: "merge-types",
        title: "Merge Types (inner/left/outer)",
        shortDescription: "Perform inner, left, and outer merges and understand the differences.",
        difficulty: "easy",
        category: "Core",
        description: `# Merge Types\n\n## Task\nImplement \`merge_ops(left, right)\` returning:\n1. Inner merge on 'id'\n2. Left merge on 'id'\n3. Outer merge on 'id'`,
        skeleton: `import pandas as pd\n\ndef merge_ops(left: pd.DataFrame, right: pd.DataFrame):\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import pandas as pd\ndef test_merge():\n    L=pd.DataFrame({'id':[1,2,3],'vl':[10,20,30]})\n    R=pd.DataFrame({'id':[2,3,4],'vr':[200,300,400]})\n    inner,left,outer=merge_ops(L,R)\n    assert len(inner)==2 and len(left)==3 and len(outer)==4\n    print("All tests passed!")`,
      },
      {
        slug: "concat-join",
        title: "Concat & Join",
        shortDescription: "Concatenate DataFrames vertically and horizontally.",
        difficulty: "easy",
        category: "Core",
        description: `# Concat & Join\n\n## Task\nImplement \`concat_join(df1, df2)\` returning:\n1. Vertical concat (axis=0), reset index\n2. Horizontal concat (axis=1)`,
        skeleton: `import pandas as pd\n\ndef concat_join(df1: pd.DataFrame, df2: pd.DataFrame):\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import pandas as pd\ndef test_concat():\n    df1=pd.DataFrame({'a':[1,2]}); df2=pd.DataFrame({'a':[3,4]})\n    v,h=concat_join(df1,df2)\n    assert len(v)==4 and h.shape==(2,2)\n    print("All tests passed!")`,
      },
    ],
  },
  {
    slug: "performance-optimization",
    subject: "pandas",
    title: "Performance Optimization",
    year: 2025,
    authors: ["PaperCode Team"],
    category: "PANDAS",
    description: "Write efficient Pandas code: vectorization, Categorical dtype, chunking, and method chaining.",
    tasks: [
      {
        slug: "categorical-dtype",
        title: "Categorical Dtype",
        shortDescription: "Convert low-cardinality string columns to Categorical to save memory.",
        difficulty: "easy",
        category: "Core",
        description: `# Categorical Dtype\n\nConverting string columns with low cardinality to \`category\` dtype reduces memory significantly.\n\n## Task\nImplement \`to_categorical(df, cols)\` converting each listed column to \`category\` dtype.`,
        skeleton: `import pandas as pd\n\ndef to_categorical(df: pd.DataFrame, cols: list) -> pd.DataFrame:\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import pandas as pd\ndef test_cat():\n    df=pd.DataFrame({'color':['red','blue','red','green']*100,'val':range(400)})\n    mem_before=df.memory_usage(deep=True).sum()\n    out=to_categorical(df,['color'])\n    assert out['color'].dtype.name=='category'\n    assert out.memory_usage(deep=True).sum()<mem_before\n    print("All tests passed!")`,
      },
      {
        slug: "method-chaining",
        title: "Method Chaining Pipeline",
        shortDescription: "Build a clean data pipeline using Pandas method chaining.",
        difficulty: "medium",
        category: "Core",
        description: `# Method Chaining\n\n## Task\nImplement \`clean_pipeline(df)\` as a single method chain:\n1. Drop rows with any NaN\n2. Rename column 'val' to 'score'\n3. Filter rows where score > 50\n4. Reset the index`,
        skeleton: `import pandas as pd\n\ndef clean_pipeline(df: pd.DataFrame) -> pd.DataFrame:\n    # YOUR CODE HERE — write as a single chain\n    raise NotImplementedError`,
        tests: `import pandas as pd\nimport numpy as np\ndef test_pipeline():\n    df=pd.DataFrame({'val':[30,None,80,20,90],'name':list('abcde')})\n    out=clean_pipeline(df)\n    assert out.index.tolist()==list(range(len(out)))\n    assert 'score' in out.columns and (out['score']>50).all()\n    print("All tests passed!")`,
      },
    ],
  },
  {
    slug: "real-world-applications",
    subject: "pandas",
    title: "Real-World Applications",
    year: 2025,
    authors: ["PaperCode Team"],
    category: "PANDAS",
    description: "Apply everything to practical problems: sales pipelines, A/B testing, and cohort analysis.",
    tasks: [
      {
        slug: "sales-analysis",
        title: "Sales Data Analysis",
        shortDescription: "Compute monthly revenue, top products, and MoM growth.",
        difficulty: "medium",
        category: "Core",
        description: `# Sales Analysis\n\n## Task\nImplement \`sales_summary(df)\` (columns: date, product, revenue).\nReturn a dict:\n- \`total_revenue\`: sum of all revenue\n- \`top_product\`: product with highest total revenue\n- \`monthly_revenue\`: Series indexed by 'YYYY-MM'`,
        skeleton: `import pandas as pd\n\ndef sales_summary(df: pd.DataFrame) -> dict:\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import pandas as pd\ndef test_sales():\n    df=pd.DataFrame({'date':['2024-01-01','2024-01-15','2024-02-01'],'product':['A','B','A'],'revenue':[100,200,150]})\n    r=sales_summary(df)\n    assert r['total_revenue']==450\n    assert r['top_product']=='A'\n    assert len(r['monthly_revenue'])==2\n    print("All tests passed!")`,
      },
      {
        slug: "ab-testing",
        title: "A/B Test Analysis",
        shortDescription: "Compute conversion rates and relative lift for an A/B test.",
        difficulty: "medium",
        category: "Core",
        description: `# A/B Test Analysis\n\n## Task\nImplement \`ab_analysis(df)\` (columns: group, converted).\nReturn a dict:\n- \`rate_A\`: conversion rate for group A\n- \`rate_B\`: conversion rate for group B\n- \`lift\`: (rate_B - rate_A) / rate_A`,
        skeleton: `import pandas as pd\n\ndef ab_analysis(df: pd.DataFrame) -> dict:\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import pandas as pd\ndef test_ab():\n    df=pd.DataFrame({'group':['A']*100+['B']*100,'converted':[1]*20+[0]*80+[1]*30+[0]*70})\n    r=ab_analysis(df)\n    assert abs(r['rate_A']-0.2)<0.001\n    assert abs(r['rate_B']-0.3)<0.001\n    assert abs(r['lift']-0.5)<0.01\n    print("All tests passed!")`,
      },
    ],
  },
];
