import type { FundTopic } from "./fundamentals";

export const pandasTopics: FundTopic[] = [
  {
    slug: "dataframe-basics",
    subject: "pandas",
    title: "DataFrame Basics",
    year: 2025,
    authors: ["PaperCode Team"],
    category: "PANDAS",
    description: "Master Pandas DataFrames and Series: creation, inspection, selection, and basic operations.",
    tasks: [
      {
        slug: "create-dataframe",
        title: "Create DataFrame from dict/list",
        shortDescription: "Build DataFrames from Python dicts and lists.",
        difficulty: "easy",
        category: "Core",
        description: `# Creating DataFrames\n\n## Task\nImplement \`create_dfs()\` returning:\n1. A DataFrame from a dict with columns \`name\`, \`age\`, \`score\`\n2. A DataFrame from a list of lists with the same columns\n3. A Series of squares from 1 to 10`,
        skeleton: `import pandas as pd\nimport numpy as np\n\ndef create_dfs():\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import pandas as pd\ndef test_create():\n    df1,df2,s=create_dfs()\n    assert list(df1.columns)==['name','age','score']\n    assert list(df2.columns)==['name','age','score']\n    assert len(s)==10 and s.iloc[-1]==100\n    print("All tests passed!")`,
      },
      {
        slug: "select-filter",
        title: "Selecting & Filtering",
        shortDescription: "Use loc, iloc, and boolean masks to select data.",
        difficulty: "easy",
        category: "Core",
        description: `# Selecting & Filtering\n\n## Task\nGiven a DataFrame with columns \`name\`, \`age\`, \`score\`, implement \`select_ops(df)\` returning:\n1. Rows where age > 25\n2. The first 3 rows using iloc\n3. Only the \`name\` and \`score\` columns`,
        skeleton: `import pandas as pd\n\ndef select_ops(df: pd.DataFrame):\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import pandas as pd\ndef test_select():\n    df=pd.DataFrame({'name':['Alice','Bob','Carol','Dave'],'age':[22,30,28,20],'score':[90,85,92,78]})\n    a,b,c=select_ops(df)\n    assert len(a)==2 and (a['age']>25).all()\n    assert len(b)==3\n    assert list(c.columns)==['name','score']\n    print("All tests passed!")`,
      },
      {
        slug: "basic-stats",
        title: "Basic Statistics",
        shortDescription: "Compute mean, median, std, and value counts on a DataFrame.",
        difficulty: "easy",
        category: "Core",
        description: `# Basic Statistics\n\n## Task\nImplement \`basic_stats(df)\` returning a dict with:\n- \`mean_score\`: mean of the score column\n- \`median_age\`: median age\n- \`top_name\`: most frequent name`,
        skeleton: `import pandas as pd\n\ndef basic_stats(df: pd.DataFrame) -> dict:\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import pandas as pd\ndef test_stats():\n    df=pd.DataFrame({'name':['Alice','Bob','Alice','Carol'],'age':[22,30,28,20],'score':[90,85,92,78]})\n    r=basic_stats(df)\n    assert abs(r['mean_score']-86.25)<0.01\n    assert r['top_name']=='Alice'\n    print("All tests passed!")`,
      },
    ],
  },
  {
    slug: "data-cleaning",
    subject: "pandas",
    title: "Data Cleaning",
    year: 2025,
    authors: ["PaperCode Team"],
    category: "PANDAS",
    description: "Handle missing data, convert types, detect duplicates, process strings.",
    tasks: [
      {
        slug: "handle-missing",
        title: "Handling Missing Values",
        shortDescription: "Detect, fill, and drop NaN values in a DataFrame.",
        difficulty: "easy",
        category: "Core",
        description: `# Handling Missing Values\n\n## Task\nImplement \`clean_missing(df)\` that:\n1. Fills numeric NaNs with column median\n2. Fills string NaNs with \`"Unknown"\`\n3. Drops rows where more than 50% of values are NaN`,
        skeleton: `import pandas as pd\n\ndef clean_missing(df: pd.DataFrame) -> pd.DataFrame:\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import pandas as pd\nimport numpy as np\ndef test_missing():\n    df=pd.DataFrame({'a':[1.0,np.nan,3.0],'b':['x',np.nan,'z']})\n    out=clean_missing(df)\n    assert out['a'].isna().sum()==0\n    assert out['b'].isna().sum()==0\n    print("All tests passed!")`,
      },
      {
        slug: "string-cleaning",
        title: "String Cleaning",
        shortDescription: "Clean string columns: strip whitespace, lowercase, replace.",
        difficulty: "easy",
        category: "Core",
        description: `# String Cleaning\n\n## Task\nImplement \`clean_strings(df, col)\` that:\n1. Strips leading/trailing whitespace\n2. Converts to lowercase\n3. Replaces multiple spaces with a single space`,
        skeleton: `import pandas as pd\n\ndef clean_strings(df: pd.DataFrame, col: str) -> pd.DataFrame:\n    # YOUR CODE HERE - return modified df\n    raise NotImplementedError`,
        tests: `import pandas as pd\ndef test_clean():\n    df=pd.DataFrame({'name':['  Alice ','BOB','Carol  Ann']})\n    out=clean_strings(df,'name')\n    assert out['name'].iloc[0]=='alice'\n    assert out['name'].iloc[1]=='bob'\n    print("All tests passed!")`,
      },
    ],
  },
  {
    slug: "groupby-operations",
    subject: "pandas",
    title: "GroupBy Operations",
    year: 2025,
    authors: ["PaperCode Team"],
    category: "PANDAS",
    description: "Group data and apply aggregations, transformations, and filters.",
    tasks: [
      {
        slug: "groupby-agg",
        title: "GroupBy Aggregation",
        shortDescription: "Group by a column and compute multiple aggregations.",
        difficulty: "easy",
        category: "Core",
        description: `# GroupBy Aggregation\n\n## Task\nImplement \`group_stats(df)\` that groups by the \`category\` column and returns a DataFrame with \`mean_score\`, \`max_score\`, and \`count\` per group.`,
        skeleton: `import pandas as pd\n\ndef group_stats(df: pd.DataFrame) -> pd.DataFrame:\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import pandas as pd\ndef test_group():\n    df=pd.DataFrame({'category':['A','B','A','B','A'],'score':[10,20,30,40,50]})\n    r=group_stats(df)\n    assert 'mean_score' in r.columns\n    assert r.loc['A','count']==3\n    print("All tests passed!")`,
      },
      {
        slug: "pivot-table",
        title: "Pivot Table",
        shortDescription: "Create a pivot table aggregating scores by category and region.",
        difficulty: "medium",
        category: "Core",
        description: `# Pivot Tables\n\n## Task\nImplement \`make_pivot(df)\` creating a pivot table with:\n- Rows: \`region\`\n- Columns: \`category\`\n- Values: mean of \`score\``,
        skeleton: `import pandas as pd\n\ndef make_pivot(df: pd.DataFrame) -> pd.DataFrame:\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import pandas as pd\ndef test_pivot():\n    df=pd.DataFrame({'region':['N','N','S','S'],'category':['A','B','A','B'],'score':[10,20,30,40]})\n    p=make_pivot(df)\n    assert p.loc['N','A']==10\n    assert p.loc['S','B']==40\n    print("All tests passed!")`,
      },
    ],
  },
  {
    slug: "time-series",
    subject: "pandas",
    title: "Time Series",
    year: 2025,
    authors: ["PaperCode Team"],
    category: "PANDAS",
    description: "Work with datetime data: parse dates, resample, rolling statistics, and handle timezones.",
    tasks: [
      {
        slug: "datetime-parsing",
        title: "Datetime Parsing & Indexing",
        shortDescription: "Parse date strings and set them as the DataFrame index.",
        difficulty: "easy",
        category: "Core",
        description: `# Datetime Parsing\n\n## Task\nImplement \`parse_timeseries(df, date_col)\` that:\n1. Converts the date column to datetime\n2. Sets it as the index\n3. Returns the data sorted by date`,
        skeleton: `import pandas as pd\n\ndef parse_timeseries(df: pd.DataFrame, date_col: str) -> pd.DataFrame:\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import pandas as pd\ndef test_parse():\n    df=pd.DataFrame({'date':['2024-03-01','2024-01-15','2024-06-20'],'val':[10,20,30]})\n    out=parse_timeseries(df,'date')\n    assert isinstance(out.index,pd.DatetimeIndex)\n    assert out['val'].iloc[0]==20\n    print("All tests passed!")`,
      },
      {
        slug: "rolling-stats",
        title: "Rolling Window Statistics",
        shortDescription: "Compute 7-day rolling mean and standard deviation.",
        difficulty: "easy",
        category: "Core",
        description: `# Rolling Statistics\n\n## Task\nImplement \`rolling_stats(s, window=7)\` returning a DataFrame with:\n- \`rolling_mean\`\n- \`rolling_std\``,
        skeleton: `import pandas as pd\n\ndef rolling_stats(s: pd.Series, window: int = 7) -> pd.DataFrame:\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import pandas as pd\nimport numpy as np\ndef test_rolling():\n    s=pd.Series(range(20),dtype=float)\n    r=rolling_stats(s,3)\n    assert 'rolling_mean' in r.columns\n    assert r['rolling_mean'].iloc[2]==1.0\n    print("All tests passed!")`,
      },
    ],
  },
];
