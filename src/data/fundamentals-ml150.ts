import type { FundTopic } from "./fundamentals";

export const ml150Topics: FundTopic[] = [
  {
    slug: "prerequisites-tensors",
    subject: "ml150",
    title: "Prerequisites & Tensors",
    year: 2025,
    authors: ["PaperCode Team"],
    category: "BASICS",
    description: "Foundational ML implementation problems focused on tensor manipulation, math operations, and broadcasting.",
    tasks: [
      {
        slug: "tensor-broadcasting",
        title: "Tensor Broadcasting Basics",
        shortDescription: "Perform tensor addition and multiplication requiring dimension alignment.",
        difficulty: "easy",
        category: "Micro",
        description: `# Tensor Broadcasting Basics\n\nBroadcast two tensors of different shapes and compute their element-wise sum and product.\n\n## Rules\n- Shapes are compared from the trailing dimension\n- A dimension of size 1 is stretched to match the other\n\n## Task\nImplement \`broadcast_ops(a, b)\` returning \`(a+b, a*b)\`.`,
        skeleton: `import numpy as np\n\ndef broadcast_ops(a: np.ndarray, b: np.ndarray):\n    """Return (a+b, a*b) using broadcasting."""\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import numpy as np\ndef test_broadcast():\n    a = np.ones((3,1)); b = np.ones((1,4))\n    s,p = broadcast_ops(a,b)\n    assert s.shape==(3,4) and p.shape==(3,4)\n    print("All tests passed!")`,
      },
      {
        slug: "matrix-multiplication",
        title: "Matrix Multiplication (Naive vs Vectorized)",
        shortDescription: "Implement matrix multiplication using naive loops and vectorized NumPy.",
        difficulty: "easy",
        category: "Micro",
        description: `# Matrix Multiplication\n\nImplement matrix multiplication two ways:\n1. **Naive**: using Python loops\n2. **Vectorized**: using \`np.matmul\` or \`@\`\n\n## Task\nReturn \`(naive_result, vectorized_result)\` — both must match.`,
        skeleton: `import numpy as np\n\ndef matmul(A: np.ndarray, B: np.ndarray):\n    """Return (naive, vectorized) matrix products."""\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import numpy as np\ndef test_matmul():\n    A=np.random.randn(4,3); B=np.random.randn(3,5)\n    naive,vec=matmul(A,B)\n    assert naive.shape==(4,5)\n    np.testing.assert_allclose(naive,vec,atol=1e-10)\n    print("All tests passed!")`,
      },
      {
        slug: "softmax-cross-entropy",
        title: "Softmax & Cross-Entropy Loss",
        shortDescription: "Implement numerically stable softmax and cross-entropy loss.",
        difficulty: "medium",
        category: "Micro",
        description: `# Softmax & Cross-Entropy\n\n## Softmax\n\`\`\`\nSoftmax(x)_i = exp(x_i - max(x)) / sum(exp(x - max(x)))\n\`\`\`\n\n## Cross-Entropy\n\`\`\`\nL = -log(softmax(logits)[target])\n\`\`\`\n\n## Task\nImplement \`softmax(x)\` and \`cross_entropy(logits, targets)\`.`,
        skeleton: `import numpy as np\n\ndef softmax(x: np.ndarray) -> np.ndarray:\n    # YOUR CODE HERE\n    raise NotImplementedError\n\ndef cross_entropy(logits: np.ndarray, targets: np.ndarray) -> float:\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import numpy as np\ndef test_softmax():\n    x=np.array([[2.,1.,0.1]])\n    s=softmax(x)\n    assert s.shape==x.shape\n    np.testing.assert_allclose(s.sum(),-1,atol=1e-6)\n    loss=cross_entropy(x,np.array([0]))\n    assert loss>0\n    print("All tests passed!")`,
      },
      {
        slug: "gradient-descent",
        title: "Gradient Descent Update",
        shortDescription: "Implement vanilla gradient descent and momentum update rules.",
        difficulty: "easy",
        category: "Micro",
        description: `# Gradient Descent\n\nVanilla update:\n\`\`\`\nθ = θ - lr * grad\n\`\`\`\n\nMomentum update:\n\`\`\`\nv = β*v + (1-β)*grad\nθ = θ - lr * v\n\`\`\`\n\n## Task\nImplement \`sgd_step\` and \`momentum_step\`.`,
        skeleton: `import numpy as np\n\ndef sgd_step(params, grads, lr=0.01):\n    # YOUR CODE HERE\n    raise NotImplementedError\n\ndef momentum_step(params, grads, v, lr=0.01, beta=0.9):\n    # YOUR CODE HERE - return (new_params, new_v)\n    raise NotImplementedError`,
        tests: `import numpy as np\ndef test_sgd():\n    p=np.array([1.0,2.0]); g=np.array([0.1,-0.1])\n    p2=sgd_step(p,g,lr=0.1)\n    np.testing.assert_allclose(p2,[0.99,2.01])\n    print("All tests passed!")`,
      },
    ],
  },
  {
    slug: "neural-network-fundamentals",
    subject: "ml150",
    title: "Neural Network Fundamentals",
    year: 2025,
    authors: ["PaperCode Team"],
    category: "BASICS",
    description: "Build and train neural networks from scratch using backpropagation.",
    tasks: [
      {
        slug: "mlp-forward",
        title: "MLP Forward Pass",
        shortDescription: "Implement a 2-layer MLP forward pass with ReLU activation.",
        difficulty: "easy",
        category: "Micro",
        description: `# MLP Forward Pass\n\nA 2-layer network:\n\`\`\`\nz1 = X @ W1 + b1\na1 = ReLU(z1)\nz2 = a1 @ W2 + b2\n\`\`\`\n\n## Task\nImplement \`mlp_forward(X, W1, b1, W2, b2)\` returning the output logits.`,
        skeleton: `import numpy as np\n\ndef mlp_forward(X, W1, b1, W2, b2):\n    """2-layer MLP forward pass."""\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import numpy as np\ndef test_mlp():\n    np.random.seed(0)\n    X=np.random.randn(8,4); W1=np.random.randn(4,8)*0.1\n    b1=np.zeros(8); W2=np.random.randn(8,3)*0.1; b2=np.zeros(3)\n    out=mlp_forward(X,W1,b1,W2,b2)\n    assert out.shape==(8,3)\n    print("All tests passed!")`,
      },
      {
        slug: "backprop-relu",
        title: "Backprop Through ReLU",
        shortDescription: "Compute the gradient of the loss through a ReLU activation.",
        difficulty: "medium",
        category: "Micro",
        description: `# Backpropagation Through ReLU\n\nReLU forward: \`a = max(0, z)\`\n\nReLU backward:\n\`\`\`\ndL/dz = dL/da * (z > 0)\n\`\`\`\n\n## Task\nImplement \`relu_backward(dout, z)\` returning \`dz\`.`,
        skeleton: `import numpy as np\n\ndef relu(z):\n    return np.maximum(0, z)\n\ndef relu_backward(dout: np.ndarray, z: np.ndarray) -> np.ndarray:\n    """Gradient of loss w.r.t z given upstream grad dout."""\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import numpy as np\ndef test_relu_backward():\n    z=np.array([-1.,0.,2.]); dout=np.ones(3)\n    dz=relu_backward(dout,z)\n    np.testing.assert_array_equal(dz,[0,0,1])\n    print("All tests passed!")`,
      },
      {
        slug: "batch-norm",
        title: "Batch Normalization",
        shortDescription: "Implement batch normalization forward pass.",
        difficulty: "medium",
        category: "Micro",
        description: `# Batch Normalization\n\n\`\`\`\nμ = mean(x)\nσ² = var(x)\nx̂ = (x - μ) / sqrt(σ² + ε)\ny = γ * x̂ + β\n\`\`\`\n\n## Task\nImplement \`batch_norm(x, gamma, beta, eps=1e-5)\`.`,
        skeleton: `import numpy as np\n\ndef batch_norm(x, gamma, beta, eps=1e-5):\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import numpy as np\ndef test_bn():\n    np.random.seed(0); x=np.random.randn(32,8)*5+3\n    g=np.ones(8); b=np.zeros(8)\n    out=batch_norm(x,g,b)\n    np.testing.assert_allclose(out.mean(0),0,atol=1e-5)\n    print("All tests passed!")`,
      },
    ],
  },
  {
    slug: "cnns",
    subject: "ml150",
    title: "CNNs",
    year: 2025,
    authors: ["PaperCode Team"],
    category: "VISION",
    description: "Build convolutional networks for image classification and detection.",
    tasks: [
      {
        slug: "conv2d-naive",
        title: "2D Convolution (Naive)",
        shortDescription: "Implement a single-channel 2D convolution using loops.",
        difficulty: "medium",
        category: "Micro",
        description: `# 2D Convolution\n\nConvolve an input image with a kernel:\n\`\`\`\nout[i,j] = sum over (ki,kj): input[i+ki, j+kj] * kernel[ki,kj]\n\`\`\`\n\n## Task\nImplement \`conv2d(image, kernel)\` with valid padding (no padding).`,
        skeleton: `import numpy as np\n\ndef conv2d(image: np.ndarray, kernel: np.ndarray) -> np.ndarray:\n    """Naive 2D convolution. image: (H,W), kernel: (kH,kW)."""\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import numpy as np\ndef test_conv():\n    img=np.ones((5,5)); k=np.ones((3,3))\n    out=conv2d(img,k)\n    assert out.shape==(3,3)\n    assert np.all(out==9)\n    print("All tests passed!")`,
      },
      {
        slug: "max-pooling",
        title: "Max Pooling",
        shortDescription: "Implement 2D max pooling with a given pool size and stride.",
        difficulty: "easy",
        category: "Micro",
        description: `# Max Pooling\n\nFor each pool window, take the maximum value:\n\`\`\`\nout[i,j] = max(input[i*s:i*s+p, j*s:j*s+p])\n\`\`\`\n\n## Task\nImplement \`max_pool2d(x, pool_size=2, stride=2)\`.`,
        skeleton: `import numpy as np\n\ndef max_pool2d(x: np.ndarray, pool_size=2, stride=2) -> np.ndarray:\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import numpy as np\ndef test_pool():\n    x=np.arange(16).reshape(4,4).astype(float)\n    out=max_pool2d(x,2,2)\n    assert out.shape==(2,2)\n    assert out[0,0]==5\n    print("All tests passed!")`,
      },
      {
        slug: "residual-block",
        title: "Residual Block",
        shortDescription: "Implement a skip-connection residual block forward pass.",
        difficulty: "medium",
        category: "Micro",
        description: `# Residual Block\n\n\`\`\`\ny = ReLU(F(x) + x)\n\`\`\`\n\nWhere F is a 2-layer linear transformation.\n\n## Task\nImplement \`residual_block(x, W1, W2)\`.`,
        skeleton: `import numpy as np\n\ndef residual_block(x, W1, W2):\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import numpy as np\ndef test_res():\n    x=np.random.randn(4,8); W1=np.zeros((8,8)); W2=np.zeros((8,8))\n    out=residual_block(x,W1,W2)\n    np.testing.assert_allclose(out,np.maximum(0,x))\n    print("All tests passed!")`,
      },
    ],
  },
  {
    slug: "rnns",
    subject: "ml150",
    title: "RNNs",
    year: 2025,
    authors: ["PaperCode Team"],
    category: "SEQUENCE",
    description: "Model sequential data with recurrent networks and gating mechanisms.",
    tasks: [
      {
        slug: "rnn-cell",
        title: "Vanilla RNN Cell",
        shortDescription: "Implement a single RNN cell forward pass.",
        difficulty: "easy",
        category: "Micro",
        description: `# Vanilla RNN Cell\n\n\`\`\`\nh_t = tanh(x_t @ Wxh + h_{t-1} @ Whh + b)\n\`\`\`\n\n## Task\nImplement \`rnn_cell(x, h_prev, Wxh, Whh, b)\` returning \`h_t\`.`,
        skeleton: `import numpy as np\n\ndef rnn_cell(x, h_prev, Wxh, Whh, b):\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import numpy as np\ndef test_rnn():\n    x=np.ones((1,4)); h=np.zeros((1,8))\n    Wxh=np.random.randn(4,8)*0.1; Whh=np.random.randn(8,8)*0.1; b=np.zeros(8)\n    ht=rnn_cell(x,h,Wxh,Whh,b)\n    assert ht.shape==(1,8)\n    assert np.all(np.abs(ht)<=1)\n    print("All tests passed!")`,
      },
      {
        slug: "lstm-cell",
        title: "LSTM Cell",
        shortDescription: "Implement the LSTM gating mechanism forward pass.",
        difficulty: "hard",
        category: "Micro",
        description: `# LSTM Cell\n\n\`\`\`\ni,f,g,o = split(linear([x,h]))\nc = sigmoid(f)*c_prev + sigmoid(i)*tanh(g)\nh = sigmoid(o)*tanh(c)\n\`\`\`\n\n## Task\nImplement \`lstm_cell(x, h_prev, c_prev, W, b)\` returning \`(h, c)\`.`,
        skeleton: `import numpy as np\n\ndef sigmoid(x): return 1/(1+np.exp(-x))\n\ndef lstm_cell(x, h_prev, c_prev, W, b):\n    """W: [(input+hidden), 4*hidden], b: [4*hidden]"""\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import numpy as np\ndef test_lstm():\n    np.random.seed(0); D,H=4,8\n    x=np.random.randn(1,D); h=np.zeros((1,H)); c=np.zeros((1,H))\n    W=np.random.randn(D+H,4*H)*0.1; b=np.zeros(4*H)\n    h2,c2=lstm_cell(x,h,c,W,b)\n    assert h2.shape==(1,H) and c2.shape==(1,H)\n    print("All tests passed!")`,
      },
    ],
  },
  {
    slug: "transformers-attention",
    subject: "ml150",
    title: "Transformers & Attention",
    year: 2025,
    authors: ["PaperCode Team"],
    category: "NLP",
    description: "Implement attention mechanisms and the full Transformer architecture.",
    tasks: [
      {
        slug: "scaled-dot-product-attention",
        title: "Scaled Dot-Product Attention",
        shortDescription: "Implement softmax(QKᵀ/√dk)V attention.",
        difficulty: "medium",
        category: "Micro",
        description: `# Scaled Dot-Product Attention\n\n\`\`\`\nAttention(Q,K,V) = softmax(QKᵀ / √d_k) · V\n\`\`\`\n\n## Task\nImplement \`attention(Q, K, V)\`.`,
        skeleton: `import numpy as np\n\ndef attention(Q, K, V, mask=None):\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import numpy as np\ndef test_attn():\n    np.random.seed(0); Q=np.random.randn(2,4,8); K=np.random.randn(2,6,8); V=np.random.randn(2,6,16)\n    out=attention(Q,K,V)\n    assert out.shape==(2,4,16)\n    print("All tests passed!")`,
      },
      {
        slug: "positional-encoding",
        title: "Sinusoidal Positional Encoding",
        shortDescription: "Generate sinusoidal positional encodings for a Transformer.",
        difficulty: "easy",
        category: "Micro",
        description: `# Positional Encoding\n\n\`\`\`\nPE(pos,2i)   = sin(pos/10000^(2i/d))\nPE(pos,2i+1) = cos(pos/10000^(2i/d))\n\`\`\`\n\n## Task\nImplement \`positional_encoding(seq_len, d_model)\`.`,
        skeleton: `import numpy as np\n\ndef positional_encoding(seq_len: int, d_model: int) -> np.ndarray:\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import numpy as np\ndef test_pe():\n    pe=positional_encoding(50,64)\n    assert pe.shape==(50,64)\n    assert -1<=pe.min() and pe.max()<=1\n    print("All tests passed!")`,
      },
      {
        slug: "layer-norm",
        title: "Layer Normalization",
        shortDescription: "Normalize across the feature dimension per sample.",
        difficulty: "easy",
        category: "Micro",
        description: `# Layer Normalization\n\n\`\`\`\nμ = mean(x, axis=-1)\nσ = std(x, axis=-1)\ny = γ*(x-μ)/(σ+ε) + β\n\`\`\`\n\n## Task\nImplement \`layer_norm(x, gamma, beta)\`.`,
        skeleton: `import numpy as np\n\ndef layer_norm(x, gamma, beta, eps=1e-5):\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import numpy as np\ndef test_ln():\n    x=np.random.randn(4,8); g=np.ones(8); b=np.zeros(8)\n    out=layer_norm(x,g,b)\n    np.testing.assert_allclose(out.mean(-1),0,atol=1e-5)\n    print("All tests passed!")`,
      },
    ],
  },
  {
    slug: "generative-models",
    subject: "ml150",
    title: "Generative Models",
    year: 2025,
    authors: ["PaperCode Team"],
    category: "GENERATIVE",
    description: "Learn GANs, VAEs, and diffusion models for image generation.",
    tasks: [
      {
        slug: "vae-reparameterization",
        title: "VAE Reparameterization Trick",
        shortDescription: "Sample from a Gaussian using the reparameterization trick.",
        difficulty: "medium",
        category: "Micro",
        description: `# Reparameterization Trick\n\nInstead of sampling z ~ N(μ,σ²) directly:\n\`\`\`\nε ~ N(0,1)\nz = μ + σ * ε\n\`\`\`\n\n## Task\nImplement \`reparameterize(mu, log_var)\` returning \`z\`.`,
        skeleton: `import numpy as np\n\ndef reparameterize(mu: np.ndarray, log_var: np.ndarray) -> np.ndarray:\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import numpy as np\ndef test_reparam():\n    mu=np.zeros((100,8)); lv=np.zeros((100,8))\n    z=reparameterize(mu,lv)\n    assert z.shape==(100,8)\n    assert not np.all(z==0)\n    print("All tests passed!")`,
      },
      {
        slug: "vae-loss",
        title: "VAE Loss (ELBO)",
        shortDescription: "Compute the VAE loss: reconstruction + KL divergence.",
        difficulty: "medium",
        category: "Micro",
        description: `# VAE Loss (ELBO)\n\n\`\`\`\nL = reconstruction_loss + KL_divergence\nKL = -0.5 * sum(1 + log_var - mu² - exp(log_var))\n\`\`\`\n\n## Task\nImplement \`vae_loss(recon, target, mu, log_var)\`.`,
        skeleton: `import numpy as np\n\ndef vae_loss(recon: np.ndarray, target: np.ndarray, mu: np.ndarray, log_var: np.ndarray) -> float:\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import numpy as np\ndef test_vae_loss():\n    mu=np.zeros((4,8)); lv=np.zeros((4,8))\n    recon=np.zeros((4,16)); target=np.zeros((4,16))\n    loss=vae_loss(recon,target,mu,lv)\n    assert loss>=0\n    print("All tests passed!")`,
      },
    ],
  },
  {
    slug: "advanced-topics",
    subject: "ml150",
    title: "Advanced Topics",
    year: 2025,
    authors: ["PaperCode Team"],
    category: "ADVANCED",
    description: "Reinforcement learning, meta-learning, model efficiency, and deployment.",
    tasks: [
      {
        slug: "policy-gradient",
        title: "REINFORCE Policy Gradient",
        shortDescription: "Compute the REINFORCE policy gradient loss.",
        difficulty: "hard",
        category: "Micro",
        description: `# REINFORCE Policy Gradient\n\n\`\`\`\nL = -sum(log_prob(a_t) * G_t)\nG_t = sum_{k=t}^T γ^(k-t) * r_k\n\`\`\`\n\n## Task\nImplement \`returns(rewards, gamma)\` and \`pg_loss(log_probs, returns)\`.`,
        skeleton: `import numpy as np\n\ndef compute_returns(rewards, gamma=0.99):\n    # YOUR CODE HERE\n    raise NotImplementedError\n\ndef pg_loss(log_probs: np.ndarray, returns: np.ndarray) -> float:\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import numpy as np\ndef test_returns():\n    r=[1,1,1]; G=compute_returns(r,0.9)\n    assert len(G)==3\n    assert G[0]>G[1]>G[2]\n    print("All tests passed!")`,
      },
      {
        slug: "knowledge-distillation",
        title: "Knowledge Distillation Loss",
        shortDescription: "Compute the soft-target KL loss for knowledge distillation.",
        difficulty: "medium",
        category: "Micro",
        description: `# Knowledge Distillation\n\nSoft targets from teacher at temperature T:\n\`\`\`\nL_soft = KL(softmax(student/T) || softmax(teacher/T)) * T²\n\`\`\`\n\n## Task\nImplement \`distillation_loss(student_logits, teacher_logits, T=4)\`.`,
        skeleton: `import numpy as np\n\ndef softmax(x, T=1.0):\n    e = np.exp((x - x.max(-1,keepdims=True))/T)\n    return e/e.sum(-1,keepdims=True)\n\ndef distillation_loss(student, teacher, T=4.0):\n    # YOUR CODE HERE\n    raise NotImplementedError`,
        tests: `import numpy as np\ndef test_distill():\n    s=np.random.randn(8,10); t=np.random.randn(8,10)\n    loss=distillation_loss(s,t)\n    assert loss>=0\n    print("All tests passed!")`,
      },
    ],
  },
];
